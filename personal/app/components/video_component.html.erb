
<% content_for :head do %>

<% end %>
<%= tag.div class: "", 
  data: {
    controller: :video, 
    video_stream_url: asset_path(key)
    } do %>
  <%= tag.video class: "video-js", data: {video_target: :video} do %>
    <source src="<%= asset_path(key) %>" type="application/vnd.apple.mpegurl">
  <% end%>
<% end %>
