
<% content_for :head do %>

<% end %>

<%= tag.div class: "h-100", 
  data: {
    controller: :video, 
    video_aspect_value: video.aspect,
    video_poster_value: video.thumbnail_key ? asset_url(video.thumbnail_key) : ''
    } do %>
  <%= tag.video class: "video-js", data: {video_target: :video} do %>
    <source src="<%= asset_url(video.key) %>" type="application/vnd.apple.mpegurl">
  <% end%>
<% end %>
