
<%= turbo_frame_tag post do %>
  <article class="archive-item">
    <time datetime="<%= post.created_at %>" class="post-date archive-date fs-4"><%= post.created_at.strftime("%B %d, %Y") %></time>
   <%= link_to post.title, post_path(post), class: 'archive-title fs-4', data: { turbo_frame: "_top" } %>
  </article>
  <%# link_to "Show", post_path(post), data: { turbo_frame: "_top" } %>
  <%# link_to "Edit", edit_post_path(post), data: { turbo_frame: "_top" } %>
  <%# button_to "Delete", post, method: :delete, class: 'btn btn-link text-danger p-0 m-0 d-inline align-baseline', form: { data: { turbo_confirm: "Are you sure?" } } %>
<% end %>

