<% content_for(:page_title) { 'All images' } %>

<table class="table table-striped table-sm">
  <thead>
    <tr>
      <% Image.attribute_names.each do |header| %>
        <th><%= header %></th>
      <% end %>
    </tr>
  <thead>
  <tbody>
    <% images.each do |image| %>
      <tr>
        <% Image.attribute_names.each do |header| %>
          <td class="text-break "><%= image.send(header) %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render images %>