<%= turbo_frame_tag "edit_asset" do %>
  <%= tag.div class: 'row justify-content-center' do %>
    <%= tag.div class: 'col-12 col-md-10 col-lg-8' do %>
      <h1>Edit asset</h1>
      <h6><%= asset.id %></h6>
      <div class="d-flex">
        <%= button_to "Destory", asset_path(asset), method: :delete, class: 'btn btn-danger btn-sm', data: {turbo_method: :delete}, form_class: ''  %>
      </div>
      <div class="card shadow-sm">
        <%= image_tag asset.thumbnail_key || asset.key, class: 'card-img-top' %>
      </div>
      <%= turbo_frame_tag asset do %>
        <%= render 'form' %>
      <% end %>
    <% end %>
  <% end %>
<% end %>