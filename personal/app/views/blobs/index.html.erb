<% content_for(:page_title) { 'All Blobs' } %>

<table class="table table-striped table-sm">
  <thead>
    <tr>
      <th>Slug</th>
      <th>Content Type</th>
      <th>Actions</th>
    </tr>
  <thead>
  <tbody>
    <%= render blobs %>
  </tbody>
</table>

<%= render PagerComponent.new(dr_pagy, previous_path: blobs_path(page: dr_pagy.prev, filter: params[:filter]), next_path: blobs_path(page: dr_pagy.next, filter: params[:filter])) %>