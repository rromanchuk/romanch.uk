<%= turbo_frame_tag "modal" do %>
  <%= render ModalComponent.new(title: pirep.raw_text, size: "modal-lg") do |component| %>
    <% component.with_body do %>
      <div data-controller="maps" data-maps-geo-json-value="<%= map_tds_pirep_path(pirep, format: :json) %>">
        <div id="map" style="width:100%; height:400px"> </div>
      </div>
    <% end %>
  <% end %>
<% end %>
