<% content_for :canonical_link_tag do %>
  <link rel="canonical" href="<%= aircraft_type_designator_url(icao_type.slug) %>" />
<% end %>
<%- content_for(:page_title) { "#{icao_type.icao_code} #{icao_type.manufacturer} #{icao_type.model}" } %>

<%= render 'disclaimer' %>

<%= render icao_type %>