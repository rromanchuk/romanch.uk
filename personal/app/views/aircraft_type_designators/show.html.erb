<%= provide(:canonical_url, aircraft_type_designator_url(icao_type.slug, host: 'romanch.uk')) %>
<%= provide(:page_title, "#{icao_type.icao_code} #{icao_type.manufacturer} #{icao_type.model}") %>

<% cache icao_type do %>
  <%= render 'disclaimer' %>
  <%= render icao_type %>
<% end %>